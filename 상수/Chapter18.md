### 1. 도커 기반 애플리케이션에서 설정을 외부로부터 주입해야 하는 가장 중요한 이유는?

A. Dockerfile을 단순화하기 위해

B. 이미지마다 환경별로 다시 빌드하기 위해

C. 하나의 이미지를 여러 환경에서 재사용하기 위해

D. 컨테이너 실행 속도를 높이기 위해

---

### 2. 도커가 애플리케이션 설정을 주입하기 위해 제공하는 기본 수단으로 옳은 것은?

A. 네트워크 포트 설정

B. 볼륨 스냅샷

C. 환경 변수와 파일 시스템

D. 이미지 레이어 병합

---

### 3. 다단 애플리케이션 설정 모델에서 **모든 환경에서 동일하지만 버전별로 달라지는 설정**은 어디에 포함시키는 것이 가장 적절한가?

A. 컨테이너 실행 시 전달하는 환경 변수

B. 이미지의 기본 설정

C. 환경별 오버라이드 설정 파일

D. 런타임 설정 API

---

### 4. 환경에 따라 달라지는 설정값(예: 환경 이름)을 다루는 방식으로 가장 적절한 것은?

A. 이미지 내부 기본 설정 파일

B. Dockerfile ARG

C. 컨테이너 파일 시스템에 주입되는 오버라이드 설정 파일

D. 애플리케이션 소스 코드에 하드코딩

---

### 5. 기능 설정(예: 통계 수집 여부)을 다루는 데 가장 적합한 방식은?

A. 이미지에 포함된 JSON 설정 파일

B. 환경 변수

C. 컨테이너 볼륨

D. Dockerfile CMD 인스트럭션

---

### 6. access-log 애플리케이션 설정 패턴에서 설정 우선순위로 옳은 것은?

A. 기본 설정 파일 > 환경 변수 > 오버라이드 설정 파일

B. 오버라이드 설정 파일 > 기본 설정 파일 > 환경 변수

C. 환경 변수 > 오버라이드 설정 파일 > 기본 설정 파일

D. 기본 설정 파일 > 오버라이드 설정 파일 > 환경 변수

---

### 7. 환경 변수를 이용해 설정을 덮어쓸 때 요구되는 조건으로 옳은 것은?

A. YAML 포맷이어야 한다

B. XML 포맷이어야 한다

C. JSON 포맷 문자열이어야 한다

D. Base64 인코딩이 필요하다

---

### 8. 환경별 설정을 패키징하는 방식의 장점으로 가장 적절한 것은?

A. 설정 유연성이 극대화된다

B. 애플리케이션 실행 시 환경 이름만 지정하면 된다

C. 민감한 정보도 이미지에 안전하게 포함할 수 있다

D. 환경 변수 사용이 불필요해진다

---

### 9. CI/CD 파이프라인에서 설정 파일을 이미지 빌드 과정에 포함시킬 때의 단점으로 옳은 것은?

A. 빌드 시간이 길어진다

B. 설정 파일을 버전 관리할 수 없다

C. 민감한 정보는 여전히 외부 주입이 필요하다

D. 단일 이미지 원칙을 유지할 수 없다

---

### 10. 레거시 애플리케이션에 도커 설정 전략을 적용할 때 사용하는 핵심 접근 방식은?

A. 애플리케이션 소스를 직접 수정한다

B. 설정 파일을 무조건 환경 변수로 대체한다

C. 설정 변환용 스크립트 또는 유틸리티를 이미지에 포함한다

D. 멀티 스테이지 빌드를 제거한다

---

---

## ✅ 정답 및 간단 해설

**1️⃣ 정답: C**

→ 설정을 외부에서 주입해야 하나의 이미지를 여러 환경에서 재사용할 수 있다.

**2️⃣ 정답: C**

→ 도커는 환경 변수와 파일 시스템을 통해 애플리케이션 환경을 구성한다.

**3️⃣ 정답: B**

→ 버전별 설정은 이미지의 기본 설정에 포함시키는 것이 적절하다.

**4️⃣ 정답: C**

→ 환경별 설정은 컨테이너 파일 시스템에 주입되는 오버라이드 파일로 관리한다.

**5️⃣ 정답: B**

→ 기능 토글 성격의 설정은 환경 변수가 가장 적합하다.

**6️⃣ 정답: C**

→ 환경 변수가 최우선이며, 그다음 오버라이드 설정, 마지막이 기본 설정이다.

**7️⃣ 정답: C**

→ 환경 변수로 설정을 덮어쓸 경우 JSON 문자열 형식이어야 한다.

**8️⃣ 정답: B**

→ 환경 이름만 지정하면 해당 설정 파일이 자동으로 적용된다.

**9️⃣ 정답: C**

→ 패스워드, API 키 같은 민감 정보는 이미지에 포함할 수 없다.

**🔟 정답: C**

→ 설정 파일과 환경 변수를 병합해 레거시 설정 형식으로 변환하는 방식이다.
