1. 리버스 프록시의 핵심 역할로 가장 적절한 것은?

A. 클라이언트가 직접 컨테이너 포트로 접근하도록 포트를 공개한다
B. 여러 웹 애플리케이션으로 가는 관문(gateway) 역할을 수행한다
C. 컨테이너 이미지를 자동 빌드한다
D. DB 쿼리를 캐싱한다

2. “리버스 프록시만 외부 포트를 공개한다”의 장점으로 가장 적절한 것은?

A. 모든 컨테이너가 80/443을 공유할 수 있다
B. 애플리케이션 컨테이너의 포트를 외부에 노출하지 않아도 되어 보안/배포가 유리하다
C. 컨테이너가 stdout 로그를 더 많이 남긴다
D. 도커 엔진이 필요 없어도 된다

3. Nginx 설정에서 proxy_pass http://whoami; 의 의미로 옳은 것은?

A. 클라이언트 요청을 whoami 업스트림으로 전달한다
B. whoami 컨테이너의 로그를 프록시로 전달한다
C. whoami 컨테이너를 자동으로 스케일링한다
D. SSL 인증서를 whoami에 자동 적용한다

4. 아래 헤더 중 “Nginx가 실제로 응답을 받아온 업스트림 IP”를 나타내도록 예시에서 사용된 것은?

A. X-Proxy
B. X-Host
C. X-Upstream
D. Strict-Transport-Security

5. 리버스 프록시에서 HTTPS를 적용할 때 일반적으로 옳은 설명은?

A. 업스트림 컨테이너마다 반드시 443 포트를 외부에 공개해야 한다
B. 인증서/키는 민감 정보이므로 비밀값 형태로 관리하는 것이 적절하다
C. 인증서는 하나면 모든 도메인에 공통으로 사용 가능하다
D. HTTPS는 프록시에서 적용할 수 없다

6. Nginx SSL 설정에서 80 → 443로 리다이렉트하는 설정의 목적은?

A. 모든 요청을 캐시로 보내기 위해
B. HTTP 접근을 HTTPS로 강제하기 위해
C. 로드밸런싱을 비활성화하기 위해
D. DNS 조회를 생략하기 위해

7. 캐싱 프록시(Nginx proxy_cache)를 사용하는 가장 큰 이점은?

A. 업스트림 컨테이너 수를 줄여도 장애가 사라진다
B. 동일 요청에 대해 업스트림 호출을 줄여 응답 시간을 낮추고 처리량을 늘릴 수 있다
C. 애플리케이션 로그를 자동으로 분류한다
D. 데이터베이스 트랜잭션이 자동으로 감소한다

8. 예시에서 X-Cache: HIT 과 함께 “X-Upstream 헤더가 들어오지 않는다”는 설명의 의미는?

A. 업스트림 컨테이너가 죽어있다
B. 응답이 프록시 캐시에서 바로 나와 업스트림까지 가지 않았다는 뜻이다
C. 로드밸런싱이 실패했다
D. 요청이 HTTPS가 아니라서 업스트림이 숨겨진다

9. Traefik(트래픽)의 특징으로 옳은 것은?

A. 컨테이너 레이블을 읽어 동적으로 라우팅 구성을 만들 수 있다
B. Nginx처럼 강력한 캐싱 프록시 기능을 기본 제공한다
C. 런타임 중 설정 변경이 불가능하다
D. Docker API를 사용할 수 없다

10. 스티키 세션(Sticky Session)이 필요한 상황으로 가장 적절한 것은?

A. 애플리케이션이 완전 무상태(stateless)라서 어느 컨테이너로 가도 상관없는 경우
B. 상태를 가진 구성 요소가 있어 특정 사용자의 요청이 계속 같은 컨테이너로 가야 하는 경우
C. 캐시를 더 오래 유지하고 싶은 경우
D. SSL 인증서를 자동 발급하고 싶은 경우

✅ 정답 & 간단 해설
문제	정답	해설
1	B	리버스 프록시는 여러 앱으로 가는 관문 역할
2	B	외부 노출을 프록시에만 집중하면 보안/업데이트/스케일링이 쉬움
3	A	proxy_pass는 업스트림으로 요청 전달
4	C	X-Upstream에 업스트림 IP가 담기도록 예시 구성
5	B	인증서/키는 민감정보라 secrets로 관리하는 게 일반적
6	B	HTTP를 HTTPS로 강제 리다이렉트
7	B	캐시로 업스트림 호출 감소 → 성능↑/부하↓
8	B	캐시 HIT이면 업스트림 호출이 없으니 업스트림 IP도 필요 없음
9	A	Traefik은 Docker API/레이블 기반 동적 설정, 자동 디스커버리 강점 (Nginx처럼 캐싱은 X)
10	B	상태가 있는 앱은 동일 사용자 요청을 같은 컨테이너로 붙여야 할 수 있음
