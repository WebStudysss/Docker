## **1. 도커 컴포즈가 개발/테스트 환경에서 특히 유용한 이유는?**

A. 운영 환경 수준의 오토스케일링을 제공하기 때문에

B. 가볍고 여러 컨테이너 애플리케이션을 단일 호스트에서 쉽게 실행할 수 있기 때문에

C. 보안 기능이 가장 강력하기 때문에

D. 쿠버네티스의 완전한 대체재이기 때문에

---

## **2. 동일한 애플리케이션을 여러 환경(개발/테스트/운영)에서 실행할 때 컨테이너 이름이 충돌하는 문제를 해결하는 올바른 방식은?**

A. 컨테이너마다 포트를 전부 다르게 한다

B. 컨테이너 내부 소스 코드를 모두 수정한다

C. 프로젝트 이름을 다르게 하거나 넘버링 방식을 사용한다

D. 도커 엔진을 환경별로 새로 설치한다

---

## **3. Docker Compose 오버라이드 파일을 사용하는 주된 목적은?**

A. 스케일링 성능을 높이기 위해

B. 여러 파일을 완전히 별개로 관리하기 위해

C. 기본 설정을 유지하면서 환경별로 필요한 변경 사항만 정의하기 위해

D. Dockerfile을 자동으로 생성하기 위해

---

## **4. 다음 중 오버라이드 파일에 대한 설명으로 옳은 것은?**

A. 항상 docker-compose.yml보다 우선순위가 낮다

B. 나중에 지정된 파일이 이전 파일의 내용을 오버라이드한다

C. 오버라이드 파일은 환경 변수만 수정할 수 있다

D. 오버라이드 파일은 단독으로 실행해야 한다

---

## **5. 오버라이드 파일을 사용하여 실행한 애플리케이션을 종료할 때 필요한 조건은?**

A. 원본 docker-compose.yml만 사용해야 종료할 수 있다

B. down 명령 시 프로젝트 이름을 명시해야 한다

C. 도커 데몬을 재시작해야 한다

D. 컨테이너 내부에서 직접 shutdown 명령을 실행해야 한다

---

## **6. Docker Compose의 ‘secrets’ 기능이 유용한 이유는?**

A. 환경변수보다 빠르게 읽히기 때문에

B. 암호화된 데이터베이스를 자동으로 생성하기 때문에

C. 민감한 정보를 안전하게 주입할 수 있고, 파일로 전달되기 때문이다

D. 비밀값을 자동으로 로테이션해 주기 때문이다

---

## **7. .env 파일 사용에 대한 올바른 설명은?**

A. 암호화되어 안전하므로 API 키 저장에 적합하다

B. 평문(text) 파일이기 때문에 민감한 정보 저장에는 적합하지 않다

C. Dockerfile 내부에서만 읽을 수 있다

D. docker-compose.yml과 함께 사용할 수 없다

---

## **8. 다음 중 '확장 필드(Extension Fields)'의 특징에 대한 설명으로 올바른 것은?**

A. 오직 도커 컴포즈에서만 사용되는 특수 문법이다

B. YAML의 사용자 정의 필드이며 이름 앞에 보통 x- 를 붙인다

C. 여러 파일 간 병합을 자동화하기 위해서만 존재한다

D. 도커 엔진이 자동으로 확장 필드를 인식해 최적화한다

---

## **9. YAML 병합 문법 `<<: *필드명` 의 역할은 무엇인가?**

A. Dockerfile 내용을 자동으로 병합한다

B. 특정 오버라이드 파일을 무시한다

C. 지정한 확장 필드 내용을 현재 블록에 병합한다

D. 지정한 컨테이너를 스케일링한다

---

## **10. 도커 기반 설정 워크플로에서 가장 중요한 개념은?**

A. 모든 설정을 이미지 안에 고정하는 것

B. 환경별 설정을 외부에서 주입하고, 컴포즈 파일을 재사용 가능한 형태로 관리하는 것

C. 개발/운영 환경 모두 스케일링을 최대화하는 것

D. Dockerfile을 가능한 많이 생성하는 것

---

---

# ✅ **정답 & 간단 해설**

| 문제 | 정답 | 해설 |
| --- | --- | --- |
| 1 | **B** | 개발/테스트 환경에서 다중 컨테이너 애플리케이션을 쉽게 실행할 수 있음 |
| 2 | **C** | Compose는 프로젝트 이름별로 컨테이너를 분리 관리함 |
| 3 | **C** | 공통 설정 유지 + 환경별 차이만 정의하기 위한 목적 |
| 4 | **B** | Compose는 나중에 지정된 파일이 먼저 것을 오버라이드함 |
| 5 | **B** | override 시 Compose는 프로젝트 이름을 기억하므로 down 시 명시해야 함 |
| 6 | **C** | secrets는 민감한 정보를 안전하게 관리하며 컨테이너 내 파일로 전달됨 |
| 7 | **B** | .env는 평문이므로 API 키 저장에 적합하지 않음 |
| 8 | **B** | 확장 필드는 YAML 사용자 정의 필드이며 x- prefix가 관례 |
| 9 | **C** | `<<: *name` 은 해당 확장 필드를 현재 블록에 병합하는 문법 |
| 10 | **B** | 환경별 설정을 외부에서 주입하고 재사용 가능한 설정 구조를 유지하는 것이 핵심 |
