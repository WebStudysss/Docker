## **1. 전통적인 서버 모니터링 방식이 컨테이너 환경과 맞지 않는 가장 큰 이유는?**

A. 서버의 운영체제가 서로 다르기 때문

B. 컨테이너는 OS 수준에서 동작하지 않기 때문

C. 컨테이너는 수십~수백 개의 인스턴스로 확장되며 동적으로 변하기 때문

D. 컨테이너는 로그를 남기지 않기 때문

---

## **2. 컨테이너 환경에서 Prometheus를 사용하는 이유로 가장 적절한 것은?**

A. 각 애플리케이션마다 서로 다른 프로토콜을 요구하기 때문이다

B. 컨테이너 IP나 개수가 변해도 동일한 형태로 메트릭을 수집할 수 있기 때문이다

C. 파일 기반 로그 수집만 가능하기 때문이다

D. 애플리케이션 내부 로그를 자동으로 저장하기 때문이다

---

## **3. Prometheus의 기본 동작 방식은 무엇인가?**

A. 대상 시스템이 메트릭을 Prometheus로 push한다

B. 중앙 서버가 각 컨테이너로 명령을 전송한다

C. Prometheus가 대상 시스템의 메트릭 엔드포인트를 주기적으로 pull한다

D. 메트릭을 DB에 자동 저장 후 push한다

---

## **4. 다음 중 Prometheus 스크래핑 대상 설정 방식이 아닌 것은?**

A. static_configs

B. dns_sd_configs

C. file_sd_configs

D. auto_pull_configs

---

## **5. dns_sd_configs를 사용하는 가장 큰 이유는?**

A. DNS를 통해 컨테이너의 환경 변수 값을 자동으로 가져오기 위해

B. DNS를 통해 자동 확장되는 컨테이너 목록을 동적으로 발견하기 위해

C. DNS 캐싱 기능을 통해 스크래핑 속도를 빠르게 하기 위해

D. DNS 방식을 통해 로그 저장소에 접근하기 위해

---

## **6. 다음 중 Prometheus에서 제공하는 메트릭 타입이 아닌 것은?**

A. Counter

B. Gauge

C. Histogram

D. Timeline

---

## **7. Prometheus에서 /metrics 엔드포인트를 제공하는 이유는?**

A. Prometheus가 애플리케이션을 직접 제어하기 위해

B. 애플리케이션이 자체적으로 로그를 수집하기 위해

C. Prometheus가 pull 방식으로 스크래핑할 수 있도록 메트릭을 노출하기 위해

D. 컨테이너 수를 자동 조절하기 위해

---

## **8. 다음 중 Prometheus로는 제공할 수 없으며, Grafana가 제공해야 하는 기능은?**

A. 메트릭 스크래핑

B. 시계열 데이터 저장

C. 대시보드 시각화

D. 메트릭 포맷 통일

---

## **9. Prometheus에서 docker 메트릭을 수집하기 위해 필요한 조건은?**

A. Docker 엔진이 Prometheus 포맷으로 메트릭을 노출해야 한다

B. Dockerfile에 Prometheus 설정을 직접 작성해야 한다

C. 컨테이너 내부에서만 수집이 가능하다

D. Docker 이미지를 Prometheus가 직접 빌드해야 한다

---

## **10. 도커 생태계를 익힐 때 중요한 핵심 역량으로 가장 적절한 것은?**

A. Dockerfile 스크립트만 잘 작성하면 된다

B. 컨테이너 기반 모니터링과 DevOps 도구들의 연동 패턴을 익히는 것

C. 서버 하나에서 여러 앱을 실행하는 법을 익히는 것

D. 시스템 콜 내부 동작을 분석하는 것

---

# ✅ **정답 및 간단 해설**

| 문제 | 정답 | 해설 |
| --- | --- | --- |
| 1 | **C** | 컨테이너는 다수의 인스턴스로 동적 생성/삭제되며, 고정 서버 기반 모니터링이 적합하지 않음 |
| 2 | **B** | Prometheus는 컨테이너 개수/IP 변화와 상관없이 동일 규격으로 메트릭을 수집 가능 |
| 3 | **C** | Prometheus는 pull 방식 스크래핑 모델 |
| 4 | **D** | auto_pull_configs라는 모드는 없음 |
| 5 | **B** | 서비스 디스커버리로 자동 확장되는 컨테이너 목록을 찾기 위함 |
| 6 | **D** | Timeline은 Prometheus 메트릭 타입이 아님 |
| 7 | **C** | Prometheus가 /metrics 같은 엔드포인트로 데이터를 pull해야 하기 때문 |
| 8 | **C** | 대시보드 시각화는 Grafana의 역할 |
| 9 | **A** | Docker 엔진이 9323 포트 등에서 Prometheus 포맷으로 메트릭을 노출해야 함 |
| 10 | **B** | 도커 생태계는 컨테이너 중심 DevOps 패턴을 이해하는 것이 핵심 |
