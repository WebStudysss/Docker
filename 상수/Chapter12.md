## **1. 운영 환경에서 오케스트레이션 도구가 필요한 가장 큰 이유는?**

A. 단일 호스트에서 실행되는 컨테이너 수가 너무 많기 때문에

B. 고가용성을 위해 여러 호스트를 클러스터로 묶고, 장애 시 자동 복구가 필요하기 때문에

C. 도커 이미지 빌드 속도가 느리기 때문에

D. 네트워크 포트를 자동으로 생성하기 위해

---

## **2. 클러스터 내부의 상태와 스케줄링 정보를 유지하는 핵심 컴포넌트는 무엇인가?**

A. 컨테이너 로그 수집기

B. 분산 데이터베이스(Cluster store)

C. Dockerfile

D. DNS Load Balancer

---

## **3. 도커 스웜에서 새로운 노드를 클러스터에 참여시키기 위해 반드시 필요한 것은?**

A. 동일한 도커 버전

B. 매니저 노드에서 발급한 참가 토큰

C. 쿠버네티스 설정 파일

D. GPU 지원

---

## **4. 도커 스웜에서 ‘서비스(service)’란 무엇을 의미하는가?**

A. 호스트 OS 위에 설치되는 백그라운드 프로그램

B. 여러 컨테이너를 하나의 논리적 단위로 다루는 오케스트레이션 객체

C. 네트워크 플러그인

D. 컨테이너 내부 포트 포워딩 규칙

---

## **5. 스웜의 레플리카(replica)에 대한 설명으로 옳은 것은?**

A. 레플리카는 스웜에서만 동작하는 특별한 컨테이너이다

B. 레플리카 수는 항상 노드 수와 동일해야 한다

C. 레플리카는 실제로는 일반 도커 컨테이너이며 자동으로 관리된다

D. 레플리카는 항상 매니저 노드에서만 실행된다

---

## **6. 스웜에서 컨테이너를 삭제했을 때 스웜이 새로운 컨테이너를 자동으로 생성하는 이유는?**

A. 스웜은 항상 노드당 컨테이너 수를 균등하게 유지해야 하기 때문

B. 서비스 정의에 명시된 ‘원하는 상태(Desired State)’를 유지하기 위해

C. 도커 엔진이 자동 업데이트 기능을 제공하기 때문

D. DNS 규칙이 깨지기 때문에

---

## **7. 다음 중 스웜의 롤링 업데이트 특징으로 올바른 것은?**

A. 전체 컨테이너가 동시에 종료된 후 신버전으로 교체된다

B. 한 레플리카씩 점진적으로 교체하며 새로운 컨테이너에 문제가 있으면 자동 중단된다

C. 업데이트 중 기존 버전의 정의는 유지되지 않는다

D. 롤백 기능은 존재하지 않는다

---

## **8. 스웜에서 오버레이 네트워크(overlay network)의 역할은?**

A. 노드 간 통신을 차단해 보안을 강화한다

B. 같은 노드에서 실행 중인 컨테이너끼리만 통신하게 한다

C. 클러스터의 모든 노드를 하나의 가상 네트워크로 연결해 트래픽을 전달한다

D. 외부 클라이언트만 연결할 수 있는 네트워크를 생성한다

---

## **9. 스웜의 인그레스 네트워킹(Ingress Networking)이 필요한 이유는?**

A. 웹 UI를 자동 생성하기 위해

B. 클러스터 외부에서 들어온 요청을 컨테이너가 실제로 실행 중인 노드로 전달하기 위해

C. DNS를 비활성화하기 위해

D. 레플리카 수를 자동 조절하기 위해

---

## **10. Docker Swarm과 Kubernetes의 비교로 옳은 설명은?**

A. Swarm은 기능이 복잡하고 Kubernetes는 단순하다

B. Swarm은 매니지드 클라우드 환경에서만 사용 가능하다

C. Kubernetes는 오픈 소스 커뮤니티 규모가 크고 대부분의 클라우드에서 매니지드 서비스로 제공된다

D. Kubernetes는 도커 컴포즈의 기능만 확장한 것이다

---

---

# ✅ **정답 & 간단 해설**

| 문제 | 정답 | 해설 |
| --- | --- | --- |
| 1 | **B** | 운영 환경은 고가용성이 필요하며 다중 호스트 관리가 필수 → 오케스트레이션 필요 |
| 2 | **B** | 클러스터 상태와 스케줄링 정보는 분산 스토어에 저장됨 |
| 3 | **B** | join을 위해 worker 또는 manager 토큰이 필수 |
| 4 | **B** | 서비스는 여러 컨테이너(레플리카)를 하나의 개념적 단위로 관리 |
| 5 | **C** | 레플리카는 특별한 것이 아닌 ‘일반 컨테이너 + 자동 관리’ |
| 6 | **B** | 스웜은 서비스 정의의 Desired State를 유지하기 때문에 자동 복구함 |
| 7 | **B** | 점진적 교체 + 신버전 이상 시 자동 중단 + 롤백 가능 |
| 8 | **C** | 오버레이 네트워크는 클러스터 전체를 하나의 가상 네트워크처럼 연결 |
| 9 | **B** | 인그레스는 외부 요청을 적절한 노드의 컨테이너로 전달하는 메커니즘 |
| 10 | **C** | Kubernetes는 대규모 커뮤니티 & 클라우드 매니지드 서비스 제공 |
