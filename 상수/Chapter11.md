## **1. 도커가 CI 과정에서 강력한 이유는 무엇인가?**

A. 모든 기술 스택에 대해 서로 다른 빌드 방식을 지원하기 때문

B. 모든 도커 프로젝트가 동일한 빌드·테스트·패키징 단계를 갖기 때문

C. 도커는 테스트 기능을 자동으로 생성해주기 때문

D. 도커는 소스 코드 없이도 이미지를 생성할 수 있기 때문

---

## **2. Docker 기반 CI 절차의 올바른 단계 순서는?**

A. 테스트 → 코드 변경 → 이미지 빌드 → 배포

B. 코드 변경 → 이미지 빌드 → 배포 → 테스트

C. 코드 변경 → CI 실행 → 빌드 → 테스트 → 배포

D. 배포 → 테스트 → 코드 변경 → 이미지 빌드

---

## **3. 도커 컴포즈가 CI 파이프라인에서 유용한 이유는?**

A. CI 서버 없이도 운영 환경 배포가 자동으로 완료되기 때문에

B. 애플리케이션 전체를 컨테이너로 실행하여 엔드-투-엔드 테스트가 가능하기 때문에

C. 도커 이미지를 자동으로 최적화해주기 때문에

D. DNS 기능을 대신해주기 때문에

---

## **4. 내부 네트워크에서 도커 기반 빌드 인프라를 구축하는 것이 적합한 상황은?**

A. 개발자가 외부 인터넷을 자주 사용해야 할 때

B. 소스 코드나 이미지가 외부로 절대 반출되어서는 안 되는 경우

C. 클라우드 기반 CI가 너무 빠른 경우

D. 하나의 이미지만 빌드하는 경우

---

## **5. Jenkins 컨테이너가 도커 명령을 실행할 수 있도록 하기 위한 핵심 설정은?**

A. Jenkins 환경변수에 DOCKER_MODE=true 추가

B. Jenkins 컨테이너에 도커 엔진을 새로 설치

C. 도커 엔진의 소켓(npipe 또는 /var/run/docker.sock)을 Jenkins 컨테이너에 마운트

D. Jenkins 이미지를 root 권한으로 실행

---

## **6. `${REGISTRY:-docker.io}` 와 같은 컴포즈 문법의 의미는?**

A. REGISTRY 환경 변수가 없어도 오류가 발생하지 않도록 한다

B. REGISTRY 값이 존재하면 docker.io를 무시한다

C. REGISTRY 값이 없을 경우 기본값으로 docker.io를 사용한다

D. REGISTRY 값이 반드시 필요하다는 의미다

---

## **7. CI 파이프라인에서 `docker-compose build --pull` 옵션을 사용하는 이유는?**

A. 빌드 속도를 빠르게 하기 위해 캐시를 모두 삭제한다

B. 기반/base 이미지가 항상 최신 버전인지 확인하기 위해

C. 로컬 이미지를 사용하지 않기 위해

D. Dockerfile을 자동 생성하기 위해

---

## **8. 의존 모듈 없이 도커만으로 CI 작업을 구성할 수 있는 이유는?**

A. 도커가 자체적으로 소스 코드 저장소 기능을 제공하기 때문

B. 모든 CI 작업을 쉘 스크립트와 도커/도커 컴포즈 명령으로 실행시킬 수 있기 때문

C. 젠킨스를 자동으로 내부에 포함하기 때문

D. 도커는 테스트 기능을 자동으로 수행하기 때문

---

## **9. 도커 기반 CI 파이프라인이 “안전한 소프트웨어 공급 체인”이라고 불리는 이유는?**

A. 빌드된 이미지를 자동으로 암호화하기 때문

B. 테스트 없이도 이미지를 배포하기 때문

C. 취약점 검사, 서명된 이미지 사용 등 신뢰성을 보장하는 절차를 포함하기 때문

D. 도커 엔진이 외부 공격을 차단하기 때문

---

## **10. CI 파이프라인 내 테스트 컨테이너의 가장 중요한 역할은?**

A. 개발 중인 소스 코드를 자동으로 수정하는 것

B. 실제 사용자 역할을 수행하며 트래픽을 생성해 엔드-투-엔드 테스트를 수행하는 것

C. 도커 네트워크를 최적화하는 것

D. 테스트 결과와 무관하게 항상 성공 처리를 반환하는 것

---

---

# ✅ **정답 & 간단 해설**

| 문제 | 정답 | 해설 |
| --- | --- | --- |
| 1 | **B** | 도커는 프로젝트와 상관없이 동일한 빌드·테스트·패키징 흐름을 제공함 |
| 2 | **C** | 코드 → CI → 빌드 → 테스트 → 배포 순서가 올바름 |
| 3 | **B** | 컴포즈로 전체 애플리케이션 + 테스트 컨테이너까지 한번에 실행 가능 |
| 4 | **B** | 보안/속도 등 이유로 내부 네트워크만 사용할 때 도커 기반 시스템이 이상적 |
| 5 | **C** | 도커 소켓 마운트는 Jenkins가 호스트 도커 엔진을 제어하게 해줌 |
| 6 | **C** | :- 문법은 “환경 변수 없으면 기본값 사용” 의미 |
| 7 | **B** | 항상 최신 보안 패치가 포함된 기반 이미지를 가져오기 위함 |
| 8 | **B** | bash/bat 스크립트 + docker compose만으로 자체 CI 구성 가능 |
| 9 | **C** | 취약점 검사·서명 이미지·정책 기반 실행 등 공급망 보안 절차 포함 |
| 10 | **B** | 테스트 컨테이너가 사용자 역할을 수행해 실제 서비스 수준의 테스트 가능 |
